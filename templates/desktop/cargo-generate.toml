[template]
cargo_generate_version = ">=0.18.0"

[placeholders.mirui-version]
type = "string"
prompt = "mirui version to depend on"
default = "0.46"
regex = "^\\d+\\.\\d+(\\.\\d+)?$"

[placeholders.backend]
type = "string"
prompt = "Desktop renderer"
choices = ["wgpu", "sdl"]
default = "wgpu"
