[package]
name = "{{project-name}}"
version = "0.1.0"
edition = "2024"
authors = ["{{authors}}"]

[dependencies]
mirui = { version = "{{mirui-version}}", features = ["sdl"] }

[profile.release]
opt-level = "s"
lto = "fat"
codegen-units = 1
strip = true
