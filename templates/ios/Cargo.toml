[package]
name = "{{project-name}}"
version = "0.1.0"
edition = "2024"
authors = ["{{authors}}"]

[lib]
crate-type = ["staticlib"]

[dependencies]
mirui = { version = "{{mirui-version}}", features = ["wgpu"] }
