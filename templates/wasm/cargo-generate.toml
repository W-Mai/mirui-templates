[template]
cargo_generate_version = ">=0.18.0"

[placeholders.mirui-version]
type = "string"
prompt = "mirui version to depend on (note: web-canvas backend not yet shipped)"
default = "0.25"
regex = "^\\d+\\.\\d+(\\.\\d+)?$"
