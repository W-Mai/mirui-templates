[package]
name = "app"
version = "0.1.0"
edition = "2024"

[features]
default = []
std = ["mirui/std"]

[dependencies]
mirui = { version = "{{mirui-version}}", default-features = false, features = ["quad-aa"] }
